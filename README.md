# Open-Source-Guard
A dashboard for tracking scope health and milestone status.

## Usage
1. Run the dashboard: `python src/dashboard.py --scope scope.json --milestones milestones.json --output dashboard.txt`
2. View the dashboard: `cat dashboard.txt`
